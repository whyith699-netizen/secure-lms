{
  "installCommand": "if [ -d web ]; then npm install && npm install --prefix web; else npm install; fi",
  "buildCommand": "if [ -d web ]; then npm run build --prefix web; else npm run build; fi",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
